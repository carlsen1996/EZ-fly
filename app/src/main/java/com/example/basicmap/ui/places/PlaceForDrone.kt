package com.example.basicmap.ui.places

import com.google.android.gms.maps.model.LatLng

class PlaceForDrone(var adresse: String, var latitudeLongitude: LatLng) { //PECULIAR NAME BECAUSE: "places" is already an abstract class.

}